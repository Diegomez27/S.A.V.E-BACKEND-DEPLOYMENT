# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5434
DB_USERNAME=postgres
DB_PASSWORD=
DB_DATABASE=SaveDB

# JWT Configuration
JWT_SECRET=
JWT_EXPIRATION=24h

# Rate Limiting (requests per TTL window)
THROTTLE_TTL=20
THROTTLE_LIMIT=100
THROTTLE_LOGIN_TTL=30
THROTTLE_LOGIN_LIMIT=100

# CORS Configuration (comma-separated)
CORS_ORIGINS=http://localhost:8100,http://localhost:4200,capacitor://localhost,http://localhost:8080,http://192.168.1.74:8100,http://192.168.1.74,http://localhost

# Timezone
TZ=America/Mazatlan